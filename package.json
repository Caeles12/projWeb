{
    "scripts": {
        "start:dev": "export QUARKUS_MODE=native; docker compose -f compose.dev.yaml up -d --build",
        "start:prod": "export QUARKUS_MODE=native; docker compose -f compose.prod.yaml up -d --build",
        "stop:dev": "docker compose -f compose.dev.yaml down",
        "stop:prod": "docker compose -f compose.prod.yaml down"
      }
      
}
